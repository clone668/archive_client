# SMSI 归档客户端配置与使用手册

本文适用于一台 Windows 电脑管理一台或多台 SMSI 采集服务器的 Google Drive
和 Cloudflare R2 归档。客户端可读取云端对象、下载到本机并进行完整校验；R2 使用
目标 Bucket 的读写 Token 时，还可以在“网盘”页面删除选中的文件或目录。

## 一、先理解账号对应关系

每台采集服务器在客户端中对应一个独立“配置”。一个配置必须绑定自己的：

- 配置 ID；
- `collector_id`；
- Google Drive `rclone` remote；
- Cloudflare R2 Endpoint、Bucket 和凭据；
- 本地归档子目录和验证报告。

推荐按下表规划，名称可以替换为你的实际名称：

| 项目 | 采集服务器一 | 采集服务器二（tencent-report） |
|---|---|---|
| 配置 ID | `tencent-paper` | `tencent-report` |
| 配置名称 | 腾讯云采集一 | `tencent-report` |
| collector ID | `tencent-paper` | `tencent-report` |
| Drive remote | `gdrive-tencent-paper:` | `gdrive-tencent-report:` |
| Google 账号 | 账号一 | 账号二 |
| R2 账号/Bucket | 账号一的归档 Bucket | 账号二的归档 Bucket |
| R2 凭据 | 配置一专属 Bucket 凭据 | 配置二专属 Bucket 凭据 |

注意：`rclone` 配置 remote 名称时不写末尾冒号，例如输入
`gdrive-tencent-paper`；客户端的“rclone Remote”字段中必须写成
`gdrive-tencent-paper:`。

两台服务器可以共用本地根目录 `D:\SMSI-Archive`。客户端会自动写入不同的
`collector=<collector_id>` 子目录，并拒绝保存重复的 `collector_id`。

## 二、账号应该使用哪一个

Google Drive 必须登录拥有归档或已获得归档读取权限的 Google 账号。它通常是
采集服务器使用的那个云盘账号，也可以是被授予读取权限的另一个账号。

Cloudflare R2 应连接采集服务器实际写入的同一个 R2 账号和 Bucket，但不要把
采集服务器的写入密钥复制到 Windows。应在 Cloudflare 中为本客户端单独创建
只限定目标 Bucket 的 S3 凭据。需要客户端删除时授予 `Object Read & Write`；
否则只授予 `Object Read`。读写凭据能够永久删除对象，必须与服务器写入密钥分开
并仅保存在受控的 Windows 用户凭据管理器中。

## 三、首次准备

1. 双击项目目录中的 `启动客户端.cmd`。该文件直接使用现有 `.venv` 运行源码，
   不会重新安装或编译。
2. 点击“设置”。
3. 保持本地目录为 `D:\SMSI-Archive`，或选择空间充足的其他磁盘目录。
4. 暂时不要点击“保存设置”，先完成 Google Drive 和 R2 配置及连接测试。

`rclone` 配置文件的默认位置是：

```text
C:\Users\huxi\AppData\Roaming\rclone\rclone.conf
```

如果当前 Windows 用户还没有配置 remote，第一次打开 `rclone config` 时看到
`No remotes found` 是正常的；已经配置过 remote 时会直接显示现有名称。

## 四、准备 Google OAuth Client ID

`rclone` 的共享 Google Client ID 将在 2026 年停止工作。持续生产使用前，应创建
自己的 OAuth Client ID。

`client_id>` 不是让你填写 Google 邮箱。停在该提示时，可以保持控制台不动，
打开浏览器完成下面步骤，创建成功后再回来粘贴。

### 1. 新建 Google Cloud 项目

1. 登录 [Google API Console](https://console.cloud.google.com/apis/dashboard)。
2. 点击页面顶部的项目选择器。
3. 点击“新建项目”。
4. 项目名称填写 `SMSI Archive Client`，然后创建并选中该项目。

创建这个项目时登录哪个 Google 账号并不决定最终读取哪个云盘。真正访问的
Google Drive 账号，是稍后浏览器 OAuth 授权时选择的账号。

### 2. 启用 Google Drive API

1. 进入“API 和服务 > 已启用的 API 和服务”。
2. 点击“启用 API 和服务”。
3. 搜索 `Google Drive API`。不要选择 `Google Cloud Storage`。
4. 打开 `Google Drive API`，点击“启用”。

### 3. 配置 OAuth 同意屏幕

1. 进入“Google Auth Platform”。如果看到“开始使用”或 `Get started`，点击它。
2. 应用名称填写 `SMSI Archive Client`。
3. 用户支持电子邮件选择你自己的邮箱。
4. `Audience` 选择 `External`。如果两个 Google 账号都属于同一个 Google
   Workspace，也可以选择 `Internal`，但这样只能授权该组织内的账号。
5. 联系邮箱填写你自己的邮箱，确认条款并创建。

### 4. 添加只读 Scope

1. 在 Google Auth Platform 左侧打开 `Data Access`。
2. 点击 `Add or remove scopes`。
3. 搜索并选择下面这个只读权限：

```text
https://www.googleapis.com/auth/drive.readonly
```

4. 点击 `Update`，然后在 Data Access 页面底部点击 `Save`。

不要选择可写的 `https://www.googleapis.com/auth/drive`。本客户端只需要列目录和
下载归档。

### 5. 添加两个 Google 账号

1. 左侧打开 `Audience`。
2. 在 `Test users` 下点击 `Add users`。
3. 把第一台和第二台服务器对应的两个 Google 账号都加入。
4. 保存。

### 6. 创建桌面应用 OAuth Client

1. 左侧打开 `Clients`，或进入“API 和服务 > 凭据”。
2. 点击 `Create OAuth client` 或“创建凭据 > OAuth 客户端 ID”。
3. Application type 选择 `Desktop app`（桌面应用）。
4. 名称填写 `SMSI Windows Archive Client`。
5. 点击创建。

创建后会显示两项：

```text
Client ID:     类似 123456789-xxxx.apps.googleusercontent.com
Client Secret: 类似 GOCSPX-xxxxxxxx
```

这两项才是 `rclone` 控制台当前要填写的内容。不要把项目编号、Google 邮箱或
Cloudflare 密钥填到这里。

### 7. 发布为 Production

如果 `Audience` 页面显示应用处于 `Testing`：

1. 点击 `Publish app`。
2. 确认发布到 `Production`。

个人自用且用户少于 100 个时通常不需要提交 Google 审核。首次登录可能出现
“Google 尚未验证此应用”，确认这是你自己创建的 `SMSI Archive Client` 后，
通过“高级”继续即可。

不要长期保持 `Testing`；外部应用在 Testing 状态下的授权通常 7 天后失效，
自动同步会随之中断。

### 8. 回到当前 `rclone` 控制台

在当前提示粘贴刚才得到的 Client ID：

```text
client_id> 123456789-xxxx.apps.googleusercontent.com
```

按 Enter 后，在下一个提示粘贴 Client Secret：

```text
client_secret> GOCSPX-xxxxxxxx
```

然后继续第五节的 `scope>` 步骤。

若现在只想临时测试，也可以让 `client_id>` 和 `client_secret>` 都留空，并在
共享 Client ID 警告处输入 `y`。但共享 Client ID 会在 2026 年停止工作，不能
作为持续生产配置。

同一个桌面 OAuth Client ID 可以用于两个 Google Drive remote；两个 remote
仍会分别保存各自账号的 OAuth Token。若两个账号属于限制不同的组织，也可以
分别创建 OAuth Client ID。

## 五、配置第一台服务器的 Google Drive

在客户端“设置 > Google Drive”中，将 `rclone Remote` 填为
`gdrive-tencent-paper:`，然后点击“配置 OAuth”。控制台按下面步骤输入。

### 1. 新建 remote

看到：

```text
No remotes found, make a new one?
n) New remote
s) Set configuration password
q) Quit config
n/s/q>
```

输入：

```text
n
```

### 2. 输入 remote 名称

看到 `name>` 时输入名称，不要写冒号：

```text
gdrive-tencent-paper
```

### 3. 选择 Google Drive

看到 `Storage>` 时输入：

```text
drive
```

如果当前版本只接受编号，就在列表中找到 `Google Drive` 对应的编号并输入。

### 4. 输入自己的 OAuth Client

看到 `client_id>` 时粘贴自己的 Google OAuth Client ID。

看到 `client_secret>` 时粘贴对应的 Client Secret。

如果这两项留空，随后会出现：

```text
Continue using the shared client_id anyway?
y) Yes
n) No (default)
y/n>
```

持续生产应输入 `n`，返回后补上自己的 Client ID。共享 Client ID 只适合临时
排查，不应作为长期配置。

### 5. 选择只读权限

看到 `scope>` 时输入：

```text
2
```

选项 2 是 `drive.readonly`，允许列目录和下载文件，但不能上传、修改或删除。

### 6. 其余提示

按下面规则回答：

| 提示 | 输入 |
|---|---|
| `service_account_file>` | 直接按 Enter，留空 |
| `Edit advanced config?` | `n` |
| `Use web browser to automatically authenticate?` | `y` |
| 浏览器登录 | 登录第一台服务器对应的 Google 账号并授权 |
| `Configure this as a Shared Drive?` | SMSI 当前部署固定输入 `n` |
| `Keep this ... remote?` | `y` |

你当前看到下面这个提示时，直接输入 `n`：

```text
Configure this as a Shared Drive (Team Drive)?
y) Yes
n) No (default)
y/n> n
```

不要在这里输入 `y`。“与我共享”也不是 Shared Drive。只有以后明确把归档迁移到
Google Workspace 专门创建的共享云端硬盘时，才需要重新配置并选择 `y`。

返回 remote 列表后应看到：

```text
Name                     Type
====                     ====
gdrive-tencent-paper     drive
```

输入 `q` 退出。

### 7. 回客户端测试

回到客户端，确认以下字段：

```text
rclone Remote: gdrive-tencent-paper:
对象前缀:      smsi/v3
rclone 路径:   rclone
```

点击“测试连接”。应显示“连接成功”，并给出可见的归档日期数量。

## 六、配置第一台服务器的 Cloudflare R2

### 1. 创建 Bucket 级 S3 凭据

1. 登录第一台服务器使用的 Cloudflare 账号。
2. 进入 `R2 Object Storage`。
3. 打开 R2 API Token/S3 凭据管理页面。
4. 如果要在客户端“网盘”页删除文件，权限选择目标归档 Bucket 的
   `Object Read & Write`；资源范围只选择这一只 Bucket，不要授权账号内所有
   Bucket。如果只需要下载和浏览，也可以使用 `Object Read`。
5. 记录 `Access Key ID` 和 `Secret Access Key`。Secret 通常只显示一次。

这里需要的是 R2 S3 凭据，不是普通 Cloudflare API Bearer Token。

### 2. 填入客户端

在“设置 > Cloudflare R2”中填写：

```text
S3 Endpoint:       https://<ACCOUNT_ID>.r2.cloudflarestorage.com
Bucket:            第一台服务器的真实归档 Bucket
对象前缀:          smsi/v3
Region:            auto
Access Key ID:     目标 Bucket 凭据的 Access Key ID
Secret Access Key: 目标 Bucket 凭据的 Secret Access Key
```

点击“测试连接”。成功后状态应同时显示“连接成功”和凭据状态。

R2 密钥不会写进 `config.json`，而是按配置 ID 分开保存在当前 Windows 用户的
凭据管理器中。

## 七、完成第一台服务器配置

切换到“常规”页，确认：

```text
配置 ID:   tencent-paper
配置名称:  自己容易识别的名称
采集流 ID: tencent-paper
本地目录:  D:\SMSI-Archive
```

`采集流 ID` 必须与云端对象路径里的 `collector=<值>` 完全一致，包括大小写。

切换到“同步策略”页，生产推荐：

```text
首选下载副本: google_drive
下载前要求两个云端 manifest 一致: 勾选
显示历史天数: 45
并发下载数: 4
```

并发数允许 `1–8`。生产默认使用 `4`，能并行下载多个中小对象，同时避免对
Google Drive API、本地磁盘和内存造成过大压力。只有在网络和磁盘均有余量时才
逐步提高；遇到限流时应恢复为 `4` 或更低。

最后点击“保存设置”，再点击主窗口的“归档”选项卡并点击“刷新”。保存结果和
配置错误会直接显示在设置页底部，不会弹出额外设置窗口。

## 八、配置第二台服务器

1. 在主界面点击“新增配置”。
2. 输入配置 ID `tencent-report`。
3. 在“常规”页确认采集流 ID 为 `tencent-report`。
4. 在 Google Drive 页使用 `gdrive-tencent-report:`。
5. 点击“配置 OAuth”，按第五节重新新建 remote。`name>` 输入
   `gdrive-tencent-report`，浏览器必须登录第二个 Google 账号。
6. 在 Cloudflare R2 页填写第二个账号的 Endpoint、Bucket 和第二套 Bucket 级 S3
   凭据。
7. 分别测试 Drive 和 R2。
8. 保存设置。

客户端顶部选择器现在应出现两个配置。切换后，标题下方的
`profile=... · collector=...` 必须同步变化。

客户端不会跨配置比较 manifest，也不会从另一台服务器的云盘回退下载。

## 九、日常使用

### 查看状态

1. 在顶部选择需要查看的采集服务器。
2. 点击“刷新”。
3. 正常日期应显示：

```text
Google Drive: 已验证
Cloudflare R2: 已验证
双副本: 一致
本地: 已验证或缺失
```

主界面每个存储位置第一项仍是当前显示范围内的已验证日期数量，同时显示：

- Google Drive：整个 Google 账号的已用和总计配额；
- Cloudflare R2：当前 Bucket 的对象数及实际占用空间、当前 collector 归档前缀
  的实际占用空间，并以每月 10 GB 免费存储额度作为容量参考；
- 本地：归档所在磁盘的已用和总计容量。

R2 的 10 GB 是每月免费存储额度，不是强制容量上限。超过后 R2 不会停止写入，而是
按 Cloudflare 当期价格计费。容量结果缓存 5 分钟，以减少 R2 列目录请求；归档状态
本身仍会正常刷新。

### 下载指定日期

1. 在表格中选择一个 UTC 日期；需要批量下载时，按住 `Ctrl` 选择不连续日期，
   或按住 `Shift` 选择连续日期。
2. 点击“下载选中”；多选时按钮会显示选中数量。
3. 客户端先读取两个云端的 `manifest.json`。
4. 两个 manifest 一致后才开始下载。
5. 客户端确认本地剩余空间足以容纳未完成对象并保留 0.25 GiB 安全空间。
6. 文件先进入 `.partial`，完成大小、SHA-256、Parquet schema、行数和业务内容
   校验后，才会原子移动到正式目录。

窗口底部会显示当前日期、下载来源、已完成对象数、活动并发数、累计容量、实时
速度、预计剩余时间和总百分比。总进度前 70% 为下载，后 30% 为完整恢复校验；
Google Drive 与 R2 都会持续更新大文件的下载进度。

批量下载按列表顺序逐日期执行，进度区同时显示当前日期序号、总体百分比和本日
百分比。某个日期失败时会继续处理其余日期，并在结束时统一列出失败详情。

下载和校验期间可以点击“取消当前任务”。正常关闭窗口时，客户端会要求确认，
随后终止正在运行的 `rclone`、等待后台线程退出，再关闭程序。已经完成的对象会
保留；R2 未完成对象可从 `.partial` 续传，Google Drive 未完成对象会在下次重新
下载。不要使用任务管理器强制结束进程，强制结束无法执行安全停止流程。

客户端和 Windows 自动任务共用本地归档根目录锁。同一目录已有下载、校验或清理
任务时，第二个任务会明确失败，不会同时写入同一个 `.partial`。

### 重新校验

选择已下载日期后点击“重新校验”，客户端会重新读取云端 manifest 并检查本地
全部文件。

### 查看文件与报告

- 本地归档栏的“打开目录”：打开当前 collector 的本地归档目录。
- “报告”页面：直接查看验证报告摘要和逐对象明细；清理计划、检查点及回执
  会显示为结构化字段。
- 报告页面的“打开文件”和“打开目录”：分别打开当前选中的原始 JSON 文件和
  当前 collector 的报告目录。
- 双击已验证日期：直接打开该日期的正式目录。

### 浏览和删除网盘文件

1. 打开主窗口“网盘”页面。
2. 点击 `Google Drive` 或 `Cloudflare R2` 切换网盘。浏览范围固定在当前配置的：

```text
<对象前缀>/collector=<当前 collector_id>/
```

3. 双击目录进入，点击“上一级”返回，点击“刷新”重新读取当前目录。
4. Google Drive 使用第五节配置的只读 OAuth，只能浏览，不能在此删除。
5. R2 选择一个文件或目录后，点击“删除选中”。客户端会递归统计精确对象数和
   容量，并在页面内显示完整 `s3://Bucket/...` 目标。
6. 核对目标后点击“确认删除”。客户端会再次读取对象清单；如果确认期间目录内容
   发生变化，会停止删除并要求重新操作。
7. 删除完成后客户端复查目标已不存在、刷新列表，并在本地报告目录保存计划和
   回执。

删除文件只会删除完全相同的对象键，不会删除具有相同开头的其他文件。删除目录
只会删除该目录精确前缀下的对象。客户端不允许跳出当前 collector 根目录，也没有
删除整个 Bucket 的入口。R2 删除是永久操作，Cloudflare 没有类似回收站的恢复步骤。

如果点击确认后显示 `AccessDenied`，说明当前 R2 Token 仍是 Object Read。回到
Cloudflare 将该 Token 改为目标 Bucket 的 `Object Read & Write`，或新建同权限的
Token 后把 Access Key ID 和 Secret 更新到“设置 > Cloudflare R2”。

## 十、自动同步所有服务器

点击“启用自动同步”会创建一个 Windows 自动任务，并立即在后台启动一次前一个
UTC 日的补齐。此后每 30 分钟检查并补齐所有已启用配置的前一个 UTC 日。主界面
会明确显示首次同步是否成功启动；任务完成后自动刷新列表，并把实际下载与校验
结果写入下方审计日志。

自动任务保持启用时，每次启动客户端并完成首次清单读取后也会立即检查一次所有
启用配置的 UTC 昨日。本地日期缺失或存在断点时会自动补齐；已经完整验证的日期
只做幂等检查，不会重复下载。若正式日期目录存在但验证标记异常，客户端会保留
现场并报告错误，不会覆盖该目录。

每个配置的“参与同步全部与 Windows 自动任务”控制它是否加入批量同步。取消
勾选后，该配置仍保留在界面中，但自动任务不会处理它。

自动任务逐个处理配置。一个配置失败不会被记录成成功，整批任务的退出码也会
是失败。审计日志位于：

```text
%LOCALAPPDATA%\SMSIArchiveClient\logs\sync-*.json
```

GUI 运行错误和启动错误记录在：

```text
%LOCALAPPDATA%\SMSIArchiveClient\logs\client.log
```

该日志按 5 MiB 轮转并保留 3 个历史文件。

## 十一、命令行用法

在客户端目录打开 PowerShell。

处理所有已启用配置的 UTC 昨日：

```powershell
.\.venv\Scripts\python.exe main.py --run-once --all-profiles
```

只处理一个配置：

```powershell
.\.venv\Scripts\python.exe main.py --run-once --profile tencent-paper
```

处理指定 UTC 日期：

```powershell
.\.venv\Scripts\python.exe main.py --run-once --all-profiles --date 2026-08-03
```

命令行模式的详细成功或失败结果写入上述 JSON 审计日志。

## 十二、本地目录结构

```text
D:\SMSI-Archive\
  collector=tencent-paper\
    .partial\
      date=2026-08-03\
    date=2026-08-02\
      business\...
      evidence\...
      raw\...
      manifest.json
      .smsi-verified.json
  collector=tencent-report\
    date=2026-08-02\...
  reports\
    collector=tencent-paper\
      verify-2026-08-02.json
    collector=tencent-report\
      verify-2026-08-02.json
```

不要手工把 `.partial` 目录改名为正式日期目录。

## 十三、常见问题

### `Config file ... not found - using defaults`

第一次运行 `rclone` 的正常提示。继续输入 `n` 新建 remote。

### `No remotes found`

当前 Windows 用户尚未创建任何 remote。按第五节从 `n` 开始配置。

### `didn't find section in config file ("gdrive")`

客户端填写的 remote 名称在 `rclone.conf` 中不存在。常见原因：

- `rclone` 中创建的是 `gdrive-tencent-paper`，客户端却填了 `gdrive:`；
- 客户端字段漏写了末尾冒号；
- 用另一个 Windows 用户创建了 remote；
- OAuth 配置没有完成就退出。

### 共享 Client ID 即将停用

说明 `client_id>` 和 `client_secret>` 被留空。持续生产应输入 `n`，按第四节创建
自己的桌面 OAuth Client ID，再重新配置 remote。

### Google Drive 测试成功但看不到日期

检查：

- 登录的 Google 账号是否正确；
- 云盘中是否存在 `smsi/v3/collector=<collector_id>/date=YYYY-MM-DD/`；
- 客户端对象前缀和 collector ID 是否与云端完全一致。

### R2 显示“凭据未保存”

输入同一配置对应的 Access Key ID 和 Secret Access Key，测试连接后保存设置。
不要把另一台服务器的 R2 凭据填进来。

### R2 测试失败

检查 Endpoint、Bucket、Bucket 凭据、对象前缀和 collector ID。Endpoint 使用 R2 S3
API 地址，不是 Cloudflare 控制台网页地址。

### “双副本 manifest 不一致”

这是保护门禁，不是客户端故障。客户端会拒绝下载，避免把两个版本的数据混在
一起。应先检查服务器归档链路，确认 Drive 和 R2 的 `manifest.json` 完全一致。

### 本地目录存在但显示“未验证”

正式目录缺少有效的 `.smsi-verified.json`，或者本地内容与云端 manifest 不匹配。
不要手工伪造标记；使用“重新校验”定位具体失败对象。

### “本地归档目录已有下载、校验或清理任务运行中”

同一本地归档根目录已经被另一个客户端窗口、命令行任务或 Windows 自动任务占用。
等待该任务结束后重试。不要同时启动多个客户端，也不要删除 `locks` 目录中的文件
来绕过保护；进程正常退出或异常结束后，操作系统会自动释放文件锁。

### “本地磁盘空间不足”

客户端按 manifest 计算尚未完成的对象容量，并额外保留 0.25 GiB 安全空间。请先
释放目标盘空间，或在设置中改用容量充足的本地目录，然后重新下载。不要手工移动
正在使用的 `.partial` 目录。

## 十四、配置与凭据备份

普通客户端配置位于：

```text
%LOCALAPPDATA%\SMSIArchiveClient\config.json
```

Google OAuth Token 位于：

```text
%APPDATA%\rclone\rclone.conf
```

`rclone.conf` 含 OAuth Token，应视为敏感文件，不要上传到公共云盘、代码仓库或
发送给他人。换电脑时更推荐重新进行 OAuth 授权。

R2 凭据位于 Windows 凭据管理器，不会随 `config.json` 一起迁移。换电脑后需要
为每个配置重新输入对应的 R2 Bucket 凭据。
